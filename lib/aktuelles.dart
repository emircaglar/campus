import 'package:flutter/cupertino.dart';

class Aktuelles extends StatelessWidget {
  const Aktuelles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
