import 'package:flutter/cupertino.dart';

class Verein extends StatelessWidget {
  const Verein({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
