import 'package:flutter/cupertino.dart';

class Lernforderung extends StatelessWidget {
  const Lernforderung({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
